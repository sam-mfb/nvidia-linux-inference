[Unit]
Description=Tailscale up with auth key
After=tailscaled.service
Requires=tailscaled.service

[Service]
Type=oneshot
RemainAfterExit=yes
ExecStart=/usr/bin/tailscale up --auth-key=file:/etc/tailscale/authkey --ssh --hostname=%H

[Install]
WantedBy=multi-user.target
